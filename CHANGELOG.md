# 更新日志

## v0.3.2（2026-09-09）

全面修复与加固版本：修复后端托盘死锁、磁贴快速添加不可见、跨窗口同步缺口等 3 项高严重度问题，及 13 项中严重度问题（数据健壮性、提醒可靠性、前端竞态），并系统性将数据库命令改为后台线程执行。

### 后端

- **修复托盘菜单与磁贴开关命令的死锁**：托盘「开关置顶磁贴」改为在 async runtime 上执行，主线程不再参与 `tile_lock` 竞争；同时避免在主线程同步创建 webview 导致的白屏。
- **全部数据库命令 async 化**：16 个同步 command 改为 `spawn_blocking` 在后台线程执行 SQLite IO，不再阻塞 Windows 主线程。
- **修复跨窗口同步缺口**：`delete_project` 解绑任务后现在会广播 `tasks-changed`；解绑与删除在事务中执行。
- **修复改期后不再提醒**：修改截止时间或重新打开任务会重置 `notified`，任务重新进入提醒流程。
- **日期字符串入库前严格校验**（chrono 解析），防止排序错乱与提醒静默失效。
- **双开保护**：新增 `tauri-plugin-single-instance`（二次启动聚焦已有窗口）；SQLite 开启 WAL + `busy_timeout(5s)`。
- 数据库文件损坏时自动备份为 `todo.db.corrupt-时间戳` 并重建，不再无法启动。
- `mark_notified` 校验任务存在性；备份文件名精确到毫秒、`exportedAt` 带时区；`save_settings` 在锁内一致地更新文件与内存。
- capabilities 按窗口最小权限拆分：磁贴不再持有通知/自启权限。
- 补充测试：sort_order 旧库迁移、改期/重开重置提醒、非法日期拒绝、设置文件损坏恢复等（Rust 侧 15 个）。

### 前端

- **修复磁贴快速添加的任务立即消失**：快速添加现在附带开始时间，创建后立即出现在磁贴列表。
- **修复「今日/逾期」跨午夜不更新**：新增共享响应式时钟（30 秒），今日列表、逾期标记、卡片颜色全部随时间自动重算。
- **复活磁贴完成条目 2 秒淡出**：原机制因已完成任务被今日列表过滤而从未生效，现基于任务状态迁移正确计时。
- 删除任务增加确认对话框（与删除项目防护一致）。
- 任务列表刷新增加序号守卫，过期 IPC 响应不再覆盖新数据。
- 提醒扫描逐任务容错、批量标记一次性刷新（消除 N+1 全量拉取）；通知权限失败不再永久缓存。
- 拖拽排序失败时回滚本地预览顺序；创建表单失败时保留输入。
- 任务详情编辑中不再被远程更新强制关闭。
- 日历支持上/下月翻页与「回到本月」，格子可键盘聚焦（附 aria-label），事件按日索引渲染。
- Ctrl+K 提升为全局快捷键；错误横幅与导航补齐 ARIA；修正卡片按钮 HTML 内容模型；全局 errorHandler 把未捕获异常送入横幅。

### 工程化

- CI 增加 concurrency 取消、超时、`cargo clippy -D warnings`、`cargo fmt --check`、`--locked`；新增 tag 触发的 NSIS 打包 Release 工作流。
- Cargo.toml 补齐 `license`/`repository`/`rust-version`，裁剪桌面端无用的 crate-type；package.json 增加 `engines`；`.gitignore` 补数据库文件安全网。

## v0.3.1（首次发布）

- 核心功能：任务/项目管理、今日/全部/日历/甘特视图、置顶磁贴、跨窗口任务同步、系统托盘、开机自启、导出备份、SQLite 存储与自动迁移、通知提醒。
